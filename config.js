/* =========================================================
   Supabase connection settings.

   Paste your two values below, from your Supabase project:
   Project Settings -> API

   These two values are SAFE to put in public code and commit
   to GitHub. The anon key only does what your Row Level
   Security policies allow. (The "service_role" key is the
   secret one — never put that in here.)
   ========================================================= */

const SUPABASE_URL = "PASTE_YOUR_PROJECT_URL_HERE";
const SUPABASE_ANON_KEY = "PASTE_YOUR_ANON_PUBLIC_KEY_HERE";
